# 命令管理器 — 全栈技术笔记

> 纯前端单文件应用 + GitHub API 云同步，零成本多设备共享数据

---

## 一、项目架构概览

```
┌──────────────────────────────────────┐
│          GitHub Pages                │
│   https://ironmole666.github.io/     │
│   linux-command/index.html           │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│           浏览器 LocalStorage         │ ← 数据缓存 + 离线存取
│  → commandManagerData (命令数组)      │
│  → github_token (GitHub PAT)         │
│  → categoryStructure (分类结构)       │
│  → lastSyncAt (最后同步时间)           │
└──────────────┬───────────────────────┘
               │
               ▼
┌──────────────────────────────────────┐
│       GitHub API (REST v3)           │ ← 云端持久化
│  GET /contents/data/data.json        │
│  PUT /contents/data/data.json        │
│  数据以 base64 编码存取               │
└──────────────────────────────────────┘
```

---

## 二、技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| **托管** | GitHub Pages | 免费静态托管，自动从 `main` 分支部署 |
| **前端** | 原生 HTML5 + CSS3 + ES6+ | 零依赖，单文件 `index.html` |
| **数据层** | `localStorage` | 离线缓存，即时读写 |
| **云端** | GitHub REST API v3 | 文件读写，base64 编码 |
| **版本控制** | Git + GitHub | 代码管理 + 数据版本历史 |
| **认证** | Fine-grained PAT | 最小权限 Token |

### 为什么选这些技术？

- **GitHub Pages** — 免费、国内可访问、自动 HTTPS、和仓库绑定
- **纯前端** — 无需服务器、无需构建、直接浏览器打开就能用
- **localStorage** — 同源策略保护、5-10MB 容量、无须后端
- **GitHub API** — 个人 Token 免费、5000 次/小时配额、数据天然有 git 历史

---

## 三、GitHub 配置详解

### 3.1 GitHub Pages 启用

```bash
# 1. 创建仓库（Public）
# 2. 推送代码到 main 分支
git remote add origin https://github.com/<user>/<repo>.git
git branch -M main
git push -u origin main

# 3. 在 GitHub 仓库 Settings → Pages 中配置：
#    Source: Deploy from a branch
#    Branch: main, / (root)
#    Save → 自动分配 URL
```

**访问地址**：`https://<user>.github.io/<repo>/`

### 3.2 Fine-grained Personal Access Token

**为什么用 Fine-grained 而不是 Classic？**
- Fine-grained 可以限制到**单个仓库**，更安全
- Fine-grained 权限粒度更细（Contents Read and write 即可）
- Fine-grained 没有过期时间（除非撤销）

**创建步骤：**
1. 打开 https://github.com/settings/tokens?type=beta
2. **Generate new token** → **Fine-grained token**
3. Token name: `命令管理器`
4. Repository access: **Only select repositories** → 选中你的仓库
5. Permissions: **Contents** → **Read and write**
6. 生成后 **立即复制**（只显示一次）

**Token 在代码中的使用：**

```javascript
// 存 localStorage（每个浏览器独立）
localStorage.setItem('github_token', token);

// 请求头格式
fetch(url, {
    headers: {
        Authorization: `Bearer ${token}`,    // Fine-grained 用 Bearer
        Accept: 'application/vnd.github.v3+json'
    }
});
```

### 3.3 GitHub API 文件读写

```javascript
// === 核心 API：仓库文件内容 ===
// GET: 读取文件（返回 base64 编码内容 + SHA）
GET /repos/{owner}/{repo}/contents/{path}

// PUT: 创建或更新文件
PUT /repos/{owner}/{repo}/contents/{path}
Body: {
    message: "commit message",
    content: base64(json),        // base64 编码的文件内容
    sha: "current_file_sha",      // 更新时必须传，创建时省略
    branch: "main"
}
```

**SHA 机制（重要）：**
- 每个文件在 git 中有一个唯一的 blob SHA
- 更新文件时必须提供当前 SHA，否则返回 **409 Conflict**
- 这个机制防止了并行写入的冲突（乐观锁）

**Base64 编码处理（注意 Unicode 兼容）：**

```javascript
// 写入：UTF-8 → base64（兼容中文）
function utf8ToBase64(str) {
    return btoa(unescape(encodeURIComponent(str)));
}

// 读取：base64 → UTF-8
function base64ToUtf8(str) {
    return decodeURIComponent(escape(atob(str)));
}
```

### 3.4 CORS 跨域注意事项

浏览器访问 `api.github.com` 属于跨域请求。关键点：

- `Authorization` 头会触发 CORS **预检请求**（OPTIONS）
- 请求头只应包含**必要**的头，不要加 `User-Agent`、`Cache-Control` 等
- 加 `?_=${Date.now()}` 时间戳参数防浏览器缓存过期 SHA

```javascript
// ✅ 正确姿势
const url = `https://api.github.com/.../data.json?_=${Date.now()}`;
fetch(url, {
    headers: {
        Authorization: `Bearer ${token}`,         // 必须
        Accept: 'application/vnd.github.v3+json' // 推荐
    }
});

// ❌ 避免的非标准头
// 'User-Agent': 'my-app'      → 禁止头，被浏览器忽略或阻塞
// 'Cache-Control': 'no-cache' → 触发额外预检，可能被拦截
```

---

## 四、前端架构模式

### 4.1 单文件 SPA

整个应用就是一个 `index.html`，分为三部分：

```
index.html
├── <style>       ← 全部 CSS（~400 行）
├── <body>        ← 全部 HTML（~100 行结构）
└── <script>      ← 全部 JS（~600 行逻辑）
```

**优缺点：**
- ✅ 部署简单，一个文件搞定
- ✅ 无构建步骤，即改即生效
- ❌ 代码量大后维护困难（适合小型工具）

### 4.2 数据模型

```javascript
// 每条命令的结构
{
    id: "uuid",              // crypto.randomUUID()
    command: "ls -la",       // 命令本身
    description: "查看文件",  // 说明
    mainCategory: "Linux",   // 主分类
    subCategory: "文件操作",  // 子分类
    tags: ["文件", "查看"],   // 标签数组
    favorite: false,         // 是否收藏
    copyCount: 0,            // 复制次数
    createdAt: "ISO日期",    // 创建时间
    updatedAt: "ISO日期"     // 更新时间
}
```

**版本迁移（向后兼容）：**

```javascript
commands.forEach(cmd => {
    if (cmd.favorite === undefined) cmd.favorite = false;
    if (cmd.copyCount === undefined) cmd.copyCount = 0;
});
```

### 4.3 localStorage 读写模式

```javascript
// 读
function loadCommands() {
    const stored = localStorage.getItem('commandManagerData');
    if (stored) {
        commands = JSON.parse(stored);
        // 兼容旧数据...
    } else {
        commands = defaultCommands;
    }
}

// 写（每次修改后都调用，确保即时持久化）
function saveToStorage() {
    localStorage.setItem('commandManagerData', JSON.stringify(commands));
}
```

**关键设计：所有修改命令的函数都必须调用 `saveToStorage()`**

### 4.4 渲染模式（全量刷新）

```javascript
function renderCommands(list) {
    // 1. 分页切片
    const pageItems = list.slice(start, start + pageSize);
    
    // 2. 一次性生成 HTML
    grid.innerHTML = pageItems.map(cmd => `
        <div class="command-card">
            ...
        </div>
    `).join('');
    
    // 3. 后处理（展开按钮的溢出检测）
    pageItems.forEach(cmd => {
        // 检查是否需要展开按钮...
    });
}
```

**优点：** 简单直接，没有 diff/patch 复杂度
**缺点：** 命令多时全量重绘有性能开销（当前 ~100 条无感知）

### 4.5 搜索模式（多词 + 模糊）

```javascript
function matchesSearch(cmd, terms) {
    // 每个词都要匹配（AND 逻辑）
    return terms.every(term => {
        // 优先精确匹配
        if (cmdExact.includes(term) || descExact.includes(term)) return true;
        // 降级模糊匹配（subsequence match）
        return fuzzyMatch(cmdText, term);
    });
}

function fuzzyMatch(text, pattern) {
    let pi = 0;  // pattern index
    for (let i = 0; i < text.length && pi < pattern.length; i++) {
        if (text[i] === pattern[pi]) pi++;
    }
    return pi === pattern.length;  // 所有字符按序找到
}
```

### 4.6 防抖搜索

```javascript
let searchTimer;
function onSearchInput() {
    clearTimeout(searchTimer);
    searchTimer = setTimeout(filterCommands, 250);
}
```

---

## 五、云同步核心逻辑

### 5.1 同步流程

```
页面加载
  ├─→ loadCommands()         ← 立即从 localStorage 加载
  ├─→ renderCommands()        ← 立即渲染
  └─→ syncPull() [后台]       ← 后台拉取 GitHub 数据

用户操作（增/删/改）
  → saveToStorage()           ← 先存本地（即时响应）
  → syncPush() [后台]         ← 再推云端（不阻塞 UI）

手动同步
  → manualSync()
    → syncPull()              ← 先拉取云端最新
    → syncPush()              ← 再推送本地修改
```

### 5.2 syncPull — 拉取云端数据

```javascript
async function syncPull() {
    // 1. 从 GitHub 读取文件
    const result = await fetchGitHubFile(token);
    
    // 2. 只覆盖本地当云端有新数据
    if (result && result.content.commands.length > 0) {
        const ghTime = new Date(result.content.updatedAt);
        const localTime = localStorage.getItem('lastSyncAt');
        
        if (!localTime || ghTime > new Date(localTime)) {
            // 云端比本地新 → 替换本地
            commands = result.content.commands;
            saveToStorage();
            filterCommands();  // 重新渲染
        }
    }
}
```

### 5.3 syncPush — 推送本地数据

```javascript
async function syncPush() {
    // 1. 先获取当前文件的 SHA（防冲突）
    const current = await fetchGitHubFile(token);
    const sha = current?.sha;
    
    // 2. 尝试推送（409 冲突时自动重试）
    for (let retry = 0; retry < 3; retry++) {
        try {
            await pushGitHubFile(commands, token, sha);
            break;
        } catch (e) {
            if (retry < 2 && e.message.includes('409')) continue;
            throw e;
        }
    }
    
    // 3. 记录同步时间
    localStorage.setItem('lastSyncAt', new Date().toISOString());
}
```

### 5.4 并发控制

```javascript
let _syncing = false;  // 全局锁

async function syncPush() {
    if (_syncing) return;  // 已有同步进行中
    _syncing = true;
    try {
        // ... 同步逻辑
    } finally {
        _syncing = false;
    }
}
```

### 5.5 云端数据格式

```json
// data/data.json
{
  "commands": [ ... ],         // 命令数组
  "updatedAt": "2026-07-02T12:00:00.000Z"  // 最后更新时间
}
```

---

## 六、UI/UX 关键技术

### 6.1 CSS 动画模式

```css
/* Toast 滑入（cubic-bezier 弹性效果） */
.toast-item {
    transform: translateX(30px);
    opacity: 0;
    transition: all 0.35s cubic-bezier(0.22, 1, 0.36, 1);
}
.toast-item.show {
    transform: translateX(0);
    opacity: 1;
}

/* Modal 缩放淡入 */
.modal-content {
    transform: scale(0.95) translateY(10px);
    transition: transform 0.25s ease;
}
.modal.active .modal-content {
    transform: scale(1) translateY(0);
}
```

### 6.2 暗色主题配色

```
背景色:    #0f172a (slate-900)
卡片色:    #1e293b (slate-800)
边框色:    #334155 (slate-700)
主色:      #0ea5e9 (sky-500)
成功色:    #10b981 (emerald-500)
危险色:    #dc2626 (red-600)
文字色:    #e2e8f0 (slate-100)
次级文字:  #94a3b8 (slate-400)
```

### 6.3 响应式断点

```css
/* 平板 */
@media (max-width: 1024px) {
    .commands-grid { grid-template-columns: repeat(auto-fill, minmax(320px, 1fr)); }
}

/* 手机 */
@media (max-width: 768px) {
    .commands-grid { grid-template-columns: 1fr; }
    .header-content { flex-direction: column; }
    .stats-bar { flex-direction: column; }
    .form-row { flex-direction: column; }
}
```

### 6.4 自定义滚动条

```css
::-webkit-scrollbar { width: 8px; }
::-webkit-scrollbar-track { background: #0f172a; }
::-webkit-scrollbar-thumb { background: #475569; border-radius: 4px; }
::-webkit-scrollbar-thumb:hover { background: #64748b; }
```

---

## 七、Git 工作流

### 7.1 本地回滚（用于功能试错）

```bash
# 撤销单个文件的所有未提交更改
git restore index.html

# 回退到某个 commit（保留修改在工作区）
git reset --soft <commit-hash>

# 查看历史
git log --oneline -10

# 查看特定 commit 的变更
git show <commit-hash> --stat
```

### 7.2 GitHub Pages 更新流程

```bash
# 修改代码 → 提交 → 推送
git add -A
git commit -m "类型: 简短描述"
git push

# 推送后等待 ~1 分钟自动部署
# 可在仓库 Actions 页查看部署状态
```

---

## 八、安全最佳实践

1. **Token 权限最小化** — Fine-grained token 只给一个仓库的 Contents 读写
2. **Token 仅存 localStorage** — 不硬编码在代码里，不写日志
3. **所有 API 请求走 HTTPS** — GitHub Pages + GitHub API 都是 HTTPS
4. **数据没有敏感信息** — 命令本身不含密码/密钥
5. **CORS 请求头精简** — 不要添加不必要的请求头，避免被浏览器拦截

---

## 九、已知限制

| 限制 | 说明 | 解决方案 |
|------|------|----------|
| 单文件代码膨胀 | 全在一个 HTML 里，超过 1500 行 | 拆分成多个文件 + 构建工具 |
| 无用户认证 | 谁有 Token 谁能改数据 | Fine-grained token + 个人使用 |
| 无冲突合并 | 后推送覆盖先推送 | 单用户场景下很少发生 |
| 无离线队列 | 离线时修改无法同步 | 加 IndexedDB + 同步队列 |
| 无实时推送 | 需要手动点同步或等页面加载 | 加 WebSocket / SSE |

---

## 十、未来扩展思路

- **WebSocket 实时同步** — 一个设备修改后其他设备自动更新
- **PWA 离线支持** — Service Worker 缓存 + 离线编辑队列
- **多用户协作** — 后端 API + 数据库（Firebase / Supabase）
- **搜索语法增强** — 支持 `tag:xxx`、`cat:xxx` 等筛选语法
- **命令执行** — 集成 xterm.js 在浏览器中直接运行命令
- **AI 推荐** — 根据使用频率和标签推荐相似命令
